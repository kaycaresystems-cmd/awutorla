// Shared active/inactive color treatment for pill-style tab switchers
// (TopNavBar, AdminSettings section switcher, DigitalJobCard's tab bar) so
// they stay visually in sync instead of each hand-rolling the same colors.
export const activeTabClass = 'bg-charcoal-950 text-mustard-400';
export const inactiveTabClass = 'text-gray-600 hover:text-charcoal-900 hover:bg-gray-100';
